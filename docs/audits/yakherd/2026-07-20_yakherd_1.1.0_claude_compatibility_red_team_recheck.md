# Yakherd 1.1.0 Claude Compatibility Red Team Recheck

- Date: 2026-07-20
- Review mode: fresh independent recheck, bounded current local bytes
- Evidence class: review
- Verdict: `pass`
- Authority effect: the independent source-correctness and release-review gate
  passes for the exact current 1.1.0 candidate; intentional staged-path review,
  commit, tag, push, and publication remain separate Git/release actions

## P0 Findings

None.

## P1 Findings

None.

## P2 Findings

None open.

### Prior P2-1: closed

The prior report found that the absolute-beginner Claude workflow treated
instruction loading as unconditional and omitted the documented project-root,
fresh-session, first-import approval, and loaded-context conditions. Current
bytes close that finding in every required layer:

- `README.md:39-42` states the execution-environment prerequisites instead of
  attributing Git and Python to the agent client.
- `README.md:48-56` qualifies Codex and Claude support by active project,
  approval, and loaded context, while keeping `AGENTS.md` the sole authority.
- The Claude copy/paste prompt at `README.md:76-88` requires a fresh session in
  the generated repository, local-target verification before approval,
  `/context` confirmation, and refusal to begin product work if either
  instruction file is absent.
- `docs/USAGE.md:10-22`, `docs/ARCHITECTURE.md:38-46`, and
  `packages/jeff_strict_ssot_v1/README.md:23-39` state the same boundary.
- The installed `template/README.md:10-27` carries the project-session,
  first-use approval, local containment, `/context`, and fail-closed work gate
  into every generated repository.

These instructions match current [Claude Code memory documentation](https://code.claude.com/docs/en/memory):
`CLAUDE.md` is loaded for a project session, `@path` imports are loaded at
launch, first external imports require approval, `@AGENTS.md` is the documented
bridge for repositories whose authority already lives in `AGENTS.md`, and
`/context` is the documented loaded-context check. The wording does not claim
that the installer can validate client approval or active session state.

## P3 Findings

None open.

### Prior P3-1: closed

The prior wording could be read as saying Codex and Claude Code bundle Git and
Python. `README.md:39-42` now says that either agent can perform the workflow
when its execution environment has Git and Python 3.11 or newer. All three
copy/paste prompts require the agent to verify those prerequisites before
cloning or writing. This is accurate and suitable for the intended absolute-
beginner audience.

## Fresh Compatibility Assessment

The current design is first-class for Codex and Claude Code without creating a
second instruction authority:

- OpenAI's current Codex manual says `AGENTS.md` loads into context
  automatically and describes repository/subdirectory scoping. The current
  README correctly qualifies this by opening the generated repository as the
  project. See [OpenAI's AGENTS.md documentation](https://developers.openai.com/codex/guides/agents-md).
- Anthropic's current documentation explicitly recommends a `CLAUDE.md`
  containing `@AGENTS.md`, including as the Windows alternative to a symlink.
- Root, source-template, and acceptance-installed `CLAUDE.md` files are each
  exactly 11 bytes: `@AGENTS.md` plus LF. Each has SHA-256
  `336cc4fbf19beaada7ccf9986414fa91851a8d7a07dfb3ccbe800a69eed0ab49`.
- The validator requires exact-case, non-reparse, in-repository `CLAUDE.md` and
  rejects content other than the `@AGENTS.md` import. The generated negative
  fixture rejects both missing and divergent adapters.
- Other coding agents and ordinary chats are described conservatively:
  generic agents must have the tools and explicitly load `AGENTS.md`; ordinary
  chat is insufficient without repository and shell access.

The beginner framing also remains accurate: Yakherd supplies governance and
reliability, the user's idea or master prompt supplies the product, and the
combination can produce usable software without guaranteeing product
correctness or safety.

## Package, Hash, And Installed-Tree Recheck

- `MANIFEST.json` and `RELEASE.json` both identify
  `jeff_strict_ssot` version `1.1.0`.
- Current `bootstrap.py` SHA-256 is
  `c41f4f5d11e5eb3bfed9eabfdef7444e28612d124a26101df8bfb3628837a8bb`.
- Current `MANIFEST.json` SHA-256 is
  `505a39636b6ec21d02d5f645716aee2af044ecf5e261d4e1773d5629e19a06fb`,
  exactly the value bound by `RELEASE.json`.
- The manifest contains exactly 43 sorted templates and pins the current
  `template/README.md` hash
  `d103d666613562d9be16192a5a23375f1427236f57dc6c803117e03f11a15ec9`.
- `python scripts\verify_release.py` passed with 43 manifest files and zero
  cache paths. `git diff --check` passed.
- Package tests passed `21/21` under `python -B`. The current acceptance record
  independently confirms that the installed generated suite passed `28/28`,
  including the Claude adapter fixture. The installed strict protocol validator
  passed with zero errors and warnings.
- Fresh acceptance at `2026-07-20T19:54:37Z` reports `passed`, expected and
  actual exits `0,0,0,0,0,0,0,2,0`, identical repeated dry runs, no dry-run
  target, zero release/record/cache errors, and zero installed missing,
  unexpected, or hash-mismatched files.
- Independent reconciliation matched the exact 49-file package tree and all 49
  recorded package hashes; all 9 aggregate-bound run-record hashes; all 18
  stdout/stderr hashes; and all 43 rendered payload hashes. The installed tree
  is exactly 44 files: 43 payload files plus the installation receipt, whose
  package version is 1.1.0.

## Safety And Invariant Recheck

The documentation correction changes only the governed template README in the
runtime payload. Installer code remains byte-unchanged. Adding `CLAUDE.md` to
the exact manifest preserves fresh no-overwrite behavior; retrofit still
requires a separately reviewed, exact allowlist and expected hashes. Runtime
remains standard-library-only and product-neutral. No installer or validator
path adds network access, dependency installation, target-repository Git
mutation, product execution, automation creation, or product-prompt ingestion.

The trust-boundary text correctly limits the retained Mosaic-origin review to
the 1.0.0 bytes and requires regenerated hashes, acceptance, and independent
review for this 1.1.0 candidate. This recheck supplies that independent review
for the exact local package bytes listed above; a reviewed Git commit/tag or
release must still authenticate `RELEASE.json` externally.

## Verdict And Release Gate

Verdict: `pass` for the exact current local Yakherd 1.1.0 Claude compatibility
candidate. Prior P2-1 and P3-1 are closed, no P0-P3 findings remain, and the
candidate is release-ready aside from intentional staged-path review, commit,
tag, push, and publication. Any subsequent candidate-byte change invalidates
this result and requires the applicable hash, acceptance, and review cycle.

## Residual Risks

- `AGENTS.md` and `CLAUDE.md` are behavioral context, not enforcement. Vendor,
  organization, user, local, ancestor, or path-scoped instructions can coexist
  or conflict, and users can decline or later exclude an import.
- Client instruction-discovery behavior can change. Compatibility claims should
  continue to be checked against current official vendor documentation.
- `/context` confirms loaded context but cannot prove that a model will follow
  every instruction. Installer and validator guarantees remain separate from
  model compliance and product correctness.
- Untracked `yakherd2.png` remains outside this candidate and this recheck.
  Explicit staging must exclude it unless separately reviewed and authorized.

## Boundary Confirmation

This recheck independently inspected the current corrected documentation,
current package and installed template README, adapter bytes, manifest/release
identity, installer/validator implications, new acceptance aggregate and run
records, and official OpenAI and Anthropic instruction-loading behavior. It ran
only bounded tests, validators, hash/tree reconciliation, and release/diff
checks. It did not repair candidate files, rewrite acceptance output, install
dependencies, run repository network code, mutate Git, stage, commit, tag,
push, publish, retrofit another repository, or run product code. The only
filesystem write is this recheck report.

human action required: review the exact staged paths, exclude unreviewed `yakherd2.png`, and authenticate the passing bytes through the separately authorized intentional Git/release workflow.
