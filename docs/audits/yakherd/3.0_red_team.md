# Yakherd 3.0 independent review

Work ID: YAKHERD-3.0. Accepted scope: `docs/3.0_IMPLEMENTATION.md`.
Reviewer: independent `v3_review` agent, read-only. Coordinator is sole writer.
Budget: R0 -> C1 -> C2 -> PASS | HUMAN_DISPOSITION_REQUIRED.

## R0

Candidate SHA-256: `5287650f9a3c26fff0230509e468f7eca18ac2081f81c4d4732ecb6ab3d2e055`.
Base: `735bd78d2790070470308d3af881a6b565d9efa3`. All 41 file hashes and the
aggregate matched. Verdict: C1 required; one P1 blocks publication.

- R0-P1-01: `init` prepended `--mode fresh` but forwarded caller arguments.
  A later `--mode retrofit` selected the generic schema-1 path; the v3 bootstrap
  also exposed it directly. Read-only argument parsing confirmed the route.
  It could render generic replacements, bypass v3 prepared-content structural
  checks, issue a v3 receipt, and remove successful backups. This violates
  accepted content-preserving migration and retained-recovery requirements.
- R0-P2-01 (advisory): the installed Y-PROC-1 policy still named three specific
  product repositories in migration guidance, contrary to product-neutral intent.

Otherwise, inspection supported lean ownership, no-overwrite setup, contained
prepared-content migration, plan binding, retained backups, rollback safeguards,
read-only diagnostics and distribution/CI wiring. Reviewer inspected passing
acceptance and installation evidence without rerunning broad tests.

## C1 authorized repair and recheck

Transition recorded before edits: R0 -> C1. Resolve R0-P1-01 by making public
v3 bootstrap/init entry points fresh-only and reserving the transaction writer
for `migrate`. Add entry-point regressions proving legacy retrofit cannot alter
target files; retain the historical V1 implementation and internal transaction
tests. Generalize the small policy paragraph for R0-P2-01.

Recheck scope: these findings and direct parser/fresh-install/migration/package
regressions, regenerated release bindings, clean v3 acceptance, rebuilt exact
wheel/sdist installations and independent focused C1 review. No scope or budget
reset.

C1 candidate SHA-256:
`65bcaf34d7ed11435e46a66a3f1d451723a69ff2e196c291e459afb1b70a6828`.
The candidate manifest retains the original R0 inventory as history. Seven
candidate files changed: CLI, bootstrap, their two regression files, the policy,
and regenerated manifest/release bindings.

Both executable paths now reject retrofit during argument parsing, and the v3
bootstrap run function contains only fresh installation. The public regression
tries both entry points with four argument forms against a reviewed schema-1
plan and checks unchanged target bytes and inventory. Existing direct writer
and v3 migration tests retain transactional coverage.

C1 validation passed: 53 root tests (two platform skips), 26 v3 installer tests,
clean v3 acceptance, exact wheel/sdist bundle verification, isolated wheel/sdist
version/setup/doctor installations, release verification and whitespace checks.
Generated evidence: `.tmp/c1-validation/`, `.tmp/acceptance-v3-c1/`,
`.tmp/distribution-smoke-c1/`.

## C1 independent verdict: PASS

Reviewer verified all 41 hashes, aggregate `65bcaf34d7ed11435e46a66a3f1d451723a69ff2e196c291e459afb1b70a6828`,
and exactly the seven recorded changes from R0. No open P0/P1 findings.

- R0-P1-01 resolved: both v3 entry points use the fresh-only parser, bootstrap
  execution has no generic retrofit branch, and independent read-only parsing
  rejected all four legacy mode/plan forms with exit 2. The regression exercises
  both executables and verifies unchanged bytes and inventory. Reviewed migrate
  retains its transaction writer.
- R0-P2-01 resolved: distributed process-policy guidance is product-neutral.

Reviewer inspected the passing C1 evidence listed above, without changing files
or rerunning broad tests. Transition: C1 -> PASS. Public commit identity and
Windows/Linux CI are publication verification steps, not a new review cycle.
