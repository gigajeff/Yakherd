# Status History

No archive exists at bootstrap because there is no prior status history.

Use `scripts/ssot/migrate_status_archive.py` only after a mature `STATUS.md`
exceeds a hard limit and a reviewed compact replacement exists. Archives are
immutable exact-byte evidence with adjacent hash/index records.
