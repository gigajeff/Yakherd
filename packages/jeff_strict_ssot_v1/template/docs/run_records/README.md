# Structured Run Records

Store bounded command-evidence JSON here. Use
`docs/templates/run_record.json`. Keep bulky raw logs/artifacts outside Git and
record exact paths and hashes. A run record supports only its named claim and
evidence class.
