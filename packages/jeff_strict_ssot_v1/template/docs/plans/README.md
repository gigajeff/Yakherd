# Architecture Plans

Store bounded, dated architecture plans here. Plans are proposals until their
gates and authority effect are explicitly accepted in `DECISIONS.md` or the
appropriate domain owner.

Plans are required only for strict mode or explicit human request. Use one
active plan path per work ID and revise it in place for the single permitted
recheck. Bounded work uses a concise human-confirmed brief instead.
